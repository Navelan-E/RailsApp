<h1>Customers</h1>
<% @customers.each do |customer| %>
  <div class = "card">
    <p><%= customer.name %></p>
    <p><%= customer.phone %></p>
    <p><%= link_to 'View Details', customers_show_path(customer.id) %></p>
  </div>
<% end %>
<hr>

