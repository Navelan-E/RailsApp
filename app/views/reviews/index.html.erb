<h1>Reviews</h1>


<hr>

<% @all_reviews.each do |review| %>
  <div class="card">
    <p><%= review.content %></p>
    <p><%= review.reviewable.mechanic.name %></p>
    <p>
      <%= link_to 'View Summary', summary_path(review.reviewable.summary) %>
    </p>
  </div>
<% end %>

<%= link_to 'Back to Summary', summaries_path %>